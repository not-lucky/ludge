"""Intentionally wrong: it finds one maximum element, not a subarray sum.

Use only with the documented stress command. It is expected to mismatch and
return CLI exit status 1 after Palestra writes a finding artifact.
"""


def solution(nums):
    return max(nums)
